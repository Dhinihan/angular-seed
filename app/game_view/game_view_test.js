'use strict';

describe('myApp.game_view module', function() {

  beforeEach(module('myApp.game_view'));

  describe('game_view controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var gameCtrl = $controller('GameCtrl');
      expect(gameCtrl).toBeDefined();
    }));

  });
});